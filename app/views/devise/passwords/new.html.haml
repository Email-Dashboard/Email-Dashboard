.uk-text-center
  %h3.uk-card-title Forgot your password?
  = simple_form_for(resource, as: resource_name, url: password_path(resource_name), class: 'uk-form-stacked') do |f|
    = f.error_notification
    .uk-form-controls.uk-margin
      = f.input :email, required: true, autofocus: true, input_html: { class: 'uk-input' }, placeholder: 'Email', label: false
    .uk-form-row
      = f.button :submit, "Send me reset password instructions", class: 'uk-button uk-button-primary'
%br
= render "devise/shared/links"
