= simple_form_for(@notification_content) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, class: 'form-control'
    = f.input :content, class: 'form-control'

  .form-actions
    = f.submit 'Save', class: 'btn btn-primary'
